[pytest]
testpaths = tests/pytest
addopts = -ra
markers =
    external: tests that call external services configured through environment variables
    integration: tests that require a running CCM Manager API
    unit: tests that do not require a running CCM Manager API
    slow: tests that are slow or require long waits
